// 이중 포인터
// 포인터 안에 포인터를 넣을 수 있다. 이것을 이중 포인터 라고 한다.
// int **q;

#include<stdio.h>

int Alpointer_main(void)
{
	//int i = 100;
	//int *p = &i;
	//int **q = &q;

	//*p = 200;	// 변수 i의 값이 200이 된다.
	//**q = 300;	// 변수 i의 값이 300이 된다.

	// 포인터 배열
	// int *ap[10] 각각 다른 포인터10개가 배열로 나열되어 있다	.
	// ex: ap[1] = apple, ap[2] = banana.....


}